#Programa 4
#Declaracion de variables
int pequeno;
int grande;
int rango;
int num;
int limite;

writeln("Ingresa "+limite+" numeros: ");
readln(num);

pequeno=num;
grande=num;

#Determinacion
#No existe la variable
while(limiteeeeee){
	readln(num);
	if(num>grande){
		grande=num;
	}
	
	if(num<pequeno){
		pequeno=num;
	}
	
	limite=limite-1;
}

#Variables no existen en las 2 siguientes lineas
rano=gre-pequ;
writeln("El rango es de:"+rang);
