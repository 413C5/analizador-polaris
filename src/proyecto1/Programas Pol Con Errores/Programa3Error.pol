#Programa 3
writeln("Impresion de numeros armstrong 1-500");
#Variable ya declarada(como tal i solo se usa en el for)
int i;

#Dentro del for, en condicion y ++, variable no declarada
for(int i=0;e<=500;e++){
	int numeroActual;
	int suma;
	numeroActual=i;
	suma=0;
	#Variable no declarada
	while(numeroAct>0){
		suma=suma+(numeroActual%10)**3;
		numeroActual=numeroActual/10;
	}
	if(suma==i){
		writeln(i);
	}
}
